export default {
  data () {
    return {
      active: 1,
      stepShow: false
    }
  },

  methods: {
    next () {
      alert()
      // this.active++
      // if (this.active ===1) {
      // this.$router.push({path: `/apply/createAppTemplate/createAppTempStep${this.active}`})
      // }
      // if (this.active++ > 2) this.active = 0
    }
  }
}
